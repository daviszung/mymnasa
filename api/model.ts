export const userModel = {
  "username": "string",      // User's username (should be unique)
  "password": "string",      // Encrypted or hashed password
  "dateCreated": "date",     // Date when the user account was created (auto-generated)
}